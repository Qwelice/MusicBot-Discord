namespace DiscordMusicBot.Enums
{
    public enum CommandType
    {
        None = 10,
        Play = 20,
        Pause = 30,
        Stop = 40,
        Next = 50,
        Back = 60,
        Join = 70,
        Leave = 80
    }
}
